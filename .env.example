# Shawarma Boss MERN Stack Environment Configuration

# Server Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Database Configuration
# Option 1: Full DATABASE_URL (recommended for production)
# DATABASE_URL=postgresql://username:password@host:port/database

# Option 2: Individual PostgreSQL connection parameters
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=shawarma_boss

# Option 3: Replit PostgreSQL (if using Replit's built-in PostgreSQL)
# DATABASE_URL will be automatically provided by Replit

# Security (for production)
# JWT_SECRET=your-super-secret-jwt-key
# BCRYPT_ROUNDS=12

# Development Settings
DEBUG=true